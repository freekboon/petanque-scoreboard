import React from "react";
import classes from "./Stats.module.scss";

const Stats = () => (
  <div className={classes.container}>
    <h1 className={classes.h1}>Stats</h1>
    <p className={classes.body}>Coming soon</p>
  </div>
);

Stats.propTypes = {};

export default Stats;
